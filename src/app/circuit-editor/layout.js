export const metadata = {
  title: 'Circuit Editor - P&ID Capture Tool',
  description: 'Circuit diagram editor for creating interactive circuit diagrams',
};

export default function CircuitEditorLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
} 